<div class="panel panel-default">
  <div class="panel-heading">急聘 <i class="fa fa-bolt"></i></div>
  <div class="panel-body">

    <% Topic.where(node_id: Node.job_id).recent.withoutDraft.limit(5).each_with_index do |topic, i| %>

        <div class="topic media topic-<%= topic.id %>">
          <div class="avatar media-left">
            <%= user_avatar_tag(topic.user, :sm) %>
          </div>
          <div class="infos media-body">
            <div class="title media-heading">
              <%= link_to(main_app.topic_path(topic), title: topic.title) do %>
                  <%= topic.title %>
              <% end %>
            </div>
          </div>
        </div>

    <% end %>
<!--    <hr>-->
<!--    <div class="input-group">-->
<!--      <input type="text" class="form-control disabled" readonly="readonly" placeholder="我要内推！">-->
<!--      <span class="input-group-btn">-->
<!--        <a href="https://jinshuju.net/f/V3TeyX" class="btn btn-default" type="button">Go!</a>-->
<!--      </span>-->
<!--    </div>-->
  </div>
</div>