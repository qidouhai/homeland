<% topic = notification.target %>
<% if topic.blank? %>
  相关信息已删除
<% else %>
  <div class="media-heading">
    <% if topic.isArticle? %>
      创建了文章 <%= article_title_tag(topic) %>
    <% else %>
      创建了帖子 <%= topic_title_tag(topic) %>
    <% end %>
  </div>
<% end %>
