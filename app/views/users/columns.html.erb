<% title_tag [@user.fullname, t('users.menu.topics')].join(' · ') %>

<div class="row">
  <%= render "sidebar", user: @user %>
  <div class="col-md-8 user-columns">
    <div class="panel panel-default">
      <div class="panel-body">
        <%= render "columns", columns: @columns, user: @user %>
      </div>
    </div>
  </div>
</div>
