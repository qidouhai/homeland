<% title_tag [@user.fullname, t('users.menu.replies')].join(' · ') %>

<div class="row">
  <%= render "sidebar", user: @user %>
  <div class="col-md-8">
    <%= render "menu" %>
    <div class="panel panel-default">
      <%= render "tip_offs", tip_offs: @tipOffs %>
      <div class="panel-footer clearfix">
        <%= paginate @tipOffs %>
      </div>
    </div>
  </div>
</div>
